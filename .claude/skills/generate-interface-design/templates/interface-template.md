# インタフェース設計書

本ドキュメントはインタフェース設計のテンプレートである。`docs/design/interface/interface-design.md` として配置し、各章を埋める。章の順序は固定（方針 → 俯瞰一覧 → 各 I/F 仕様 → エラー方針 → セキュリティ → 注意事項）。

- 対象システム名: {{SYSTEM_NAME}}
- 対応する要件定義書: [`docs/requirements/functional.md`](../../requirements/functional.md) / `docs/requirements/usecase/` / [`docs/requirements/non-functional.md`](../../requirements/non-functional.md)
- 対応するアーキテクチャ設計書: [`docs/design/architecture/architecture.md`](../architecture/architecture.md)
- 対応するデータ設計書: [`docs/design/data/data-design.md`](../data/data-design.md)
- 作成日 / 最終更新日: YYYY-MM-DD / YYYY-MM-DD
- 版: 0.1（ドラフト）

---

## 1. I/F 設計方針

本インタフェース設計全体を貫く方針を箇条書きで示す。論理契約レベルの判断のみを扱い、製品選定や具体設定値には踏み込まない。

- **API 呼出様式**: {{REST / RPC / GraphQL / メッセージング のいずれを基本とするか。混在時の判定基準}}
- **人間向け I/F の種別適用方針**: 本システムで採用する種別を列挙し、それぞれの採用理由を要件 ID に紐付ける。採用しない種別は「採用しない」と明記する。
  - 画面: {{採用 / 不採用、採用時の主対象利用者}}
  - 帳票: {{採用 / 不採用、採用時のフォーマット方針（CSV / Excel / PDF 等）}}
  - 通知: {{採用 / 不採用、採用時のチャネル種別（メール / SMS / プッシュ / 社内チャット 等）}}
  - CLI: {{採用 / 不採用、採用時の対象（エンドユーザ / 運用者 / 開発者）}}
  - 音声（VUI / IVR）: {{採用 / 不採用、採用時の主チャネル（電話 / スマートスピーカー / 車載 等）}}
  - チャット・メールボット: {{採用 / 不採用、採用時の主チャネル種別}}
- **バリデーションの責務分離**: 人間向け I/F 側は UX 向上のためのクライアント検証、API 側は業務正当性の最終判断。前者を通過してもサーバ側で必ず再検証する。CLI・音声・ボットなど表現力の違う入力チャネルでも、サーバ側検証が最終判断である原則は変えない。
- **認証認可の位置**: {{IdP 委譲 / ゲートウェイ集約 / 自前 のうち、アーキ側で決定された責務位置を再掲し、各 API への適用方針を示す}}
- **エラー表現の統一**: {{機械可読コード＋人間可読メッセージ＋詳細 を含む統一構造で全 API 揃える / HTTP ステータスのみで表現 など}}
- **同期／非同期の境界適用**: {{アーキ側の境界を I/F 単位に落とし込んだ方針}}
- **タイムアウト・リトライの基本方針**: {{画面同期 API は短め・リトライなし / バッチは長め・全体リトライ、など方針レベル。具体秒数は詳細設計}}
- **機微情報の取り扱い**: {{リクエスト・レスポンス・ログでのマスキング原則、平文授受可否}}
- **ファイル／帳票の文字コード・改行コード原則**: {{UTF-8 BOM なし・LF 基本。既存システム都合での例外の判定基準}}

---

## 2. インタフェース一覧

### 2.1 人間向けインタフェース一覧（UI-NNN）

要件定義書の `UI-NNN` を引き継ぐ。新規採番は要件側で行う。画面・帳票・通知に加え、CLI・音声・チャット・メールなど「人が発信者または受信者になる I/F」全般を本表で扱う。

| UI-ID | 名称 | 種別 | 対象利用者 | チャネル | 主な起動 API | 関連 UC | 関連 ENT |
|-------|------|------|------------|-----------|----------------|---------|----------|
| UI-001 | {{画面名}} | 画面 | {{利用者}} | ブラウザ | API-001, API-002 | UC-001 | ENT-001, ENT-002 |
| UI-0XX | {{帳票名}} | 帳票 | {{利用者}} | ダウンロード（CSV / Excel） | API-0XX | UC-00X | ENT-00X |
| UI-0XX | {{通知名}} | 通知 | {{利用者}} | メール / 社内チャット | API-0XX | UC-00X | ENT-00X |
| UI-0XX | {{CLI 名}} | CLI | 運用者 | ターミナル | API-0XX | UC-00X | ENT-00X |
| UI-0XX | {{音声 I/F 名}} | 音声 | {{利用者}} | 電話 / スマートスピーカー | API-0XX | UC-00X | ENT-00X |
| UI-0XX | {{ボット名}} | チャット / メール | {{利用者}} | Slack / LINE / メール | API-0XX | UC-00X | ENT-00X |

メモ:
- 種別は `画面 / 帳票 / 通知 / CLI / 音声 / チャット / メール / その他` を使う。本プロジェクトで採用しない種別は、1 章で不採用と宣言した上で本表から外す（削除の痕跡を備考に残さなくてよい）。
- CLI・音声・ボットを採用しないプロジェクトでも、本表は削らずに種別列の説明は残し、行だけ省略する。

### 2.2 API インタフェース一覧（API-NNN）

本ドキュメントで新規採番する。`API-001` から。

| API-ID | 名称 | メソッド | 論理パス | 目的 | 認証 | 同期・非同期 | 発側 COMP | 受側 COMP | 関連 UI・UC・SIF | 関連 LNK |
|--------|------|----------|----------|------|------|---------------|-------------|-------------|---------------------|----------|
| API-001 | {{名称}} | POST | /resources | {{目的}} | 必須 / 不要 / 内部のみ | 同期 / 非同期 | COMP-002 | COMP-003 | UI-001, UC-001 | LNK-003 |
| API-002 |   |   |   |   |   |   |   |   |   |   |

### 2.3 システム間インタフェース一覧（SIF-NNN）

要件定義書の `SIF-NNN` を引き継ぐ。新規採番は要件側で行う。

| SIF-ID | 連携先システム | 連携方向 | 連携方式 | 連携契機 | 想定頻度・データ量 | 同期・非同期 | 正本 | 関連 UC・UI・LNK |
|--------|----------------|----------|-----------|-----------|----------------------|---------------|------|---------------------|
| SIF-001 | {{システム名}} | 受信 / 送信 / 双方向 | ファイル / API / メッセージング | 人手 / スケジュール / イベント | 月 1 回 / 約 100 行 | 非同期 | 自システム / 外部 / 協調 | UC-010, UI-010, LNK-007 |
| SIF-002 |   |   |   |   |   |   |   |   |

### 2.4 通信インタフェース一覧（CIF-NNN）

要件定義書の `CIF-NNN` を再掲し、本ドキュメントの API・SIF との紐付けを確認する。

| CIF-ID | 通信相手 | プロトコル | 暗号化・認証 | 紐付く API-NNN・SIF-NNN |
|--------|----------|------------|---------------|---------------------------|
| CIF-001 | 利用者ブラウザ ⇔ 本システム | HTTPS | TLS / セッション認証 | API-001, API-002, … |
| CIF-002 | 本システム ⇔ 外部クラウドストレージ | HTTPS / SDK | クラウド標準 | SIF-003 |
| CIF-003 |   |   |   |   |

メモ:
- `CIF-NNN` に紐付く I/F が本ドキュメントに存在しない場合、通信経路だけが浮いている可能性がある。「6.1 リスク・懸念点」に整合漏れとして記録する。

---

## 3. 各インタフェース仕様

すべての I/F で同じサブテンプレートに沿って記述する。該当なしの節は「該当なし」と明記して省略しない。

### 3.1 人間向けインタフェース仕様（UI-NNN）

採用する種別ごとにサブ節で記述する。採用しない種別はサブ節ごと省略してよい（1 章で不採用宣言されていれば、読み手はそれで十分理解できる）。

#### 3.1.1 画面仕様

##### UI-001 {{画面名}}（画面）

**概要**: {{画面の目的・対象利用者・代表的なユースケース。関連 UC-NNN を明記}}

**入力項目**:

| 項目名（論理） | 論理型 | 必須 | 既定値 | 値域・選択肢 | 対応 ENT・属性 | バリデーション ID |
|-----------------|--------|------|--------|----------------|------------------|---------------------|
| {{項目名}} | 文字列 / 整数 / 数値 / 真偽値 / 日付 / 日時 / 列挙 / 参照（ENT-NNN） | ○ / △（条件付き）/ − | {{既定値 or 空}} | {{範囲・列挙値}} | ENT-001.属性名 | VR-001 |

**出力項目**:

| 項目名（論理） | 論理型 | 源泉 | 空欄時の表示 | 概要 |
|-----------------|--------|------|-----------------|------|
| {{項目名}} | {{論理型}} | ENT-001.属性名 / 派生（式の概要）/ API-00X 応答 | {{ハイフン / 空白 / 0 等}} | {{意味・用途}} |

**画面イベントと起動 API**:

| イベント | 起動 API | 事前条件 | 事後の画面遷移 | 想定エラー |
|-----------|-----------|-----------|------------------|-------------|
| [保存] ボタン押下 | API-001 | 入力値がクライアント検証を通過 | 同画面に成功トースト、編集中フラグ解除 | ERR-001, ERR-004 |
| [検索] ボタン押下 | API-002 | 検索条件のうち必須項目が入力済み | 同画面内の一覧更新 | ERR-001 |
| 画面初期表示 | API-003 | 認証済み | − | ERR-002, ERR-005 |

**画面間遷移**: {{どの画面から来るか・どの画面へ抜けるか。要件側の画面遷移図との差分があれば明記}}

**備考**: {{論理契約レベルの補足のみ。UI レイアウト詳細は詳細設計に回す}}

#### 3.1.2 帳票仕様

##### UI-0XX {{帳票名}}（帳票）

**概要**: {{帳票の目的・対象利用者・関連 UC-NNN}}

**抽出条件**: {{対象期間 / 対象レコードの絞り込みパラメータ。画面起点なら対応する画面入力項目、バッチ起点ならスケジュールと既定パラメータ}}

**出力項目**:

| 項目名（論理） | 論理型 | 源泉 | 空欄時の表現 | 概要 |
|-----------------|--------|------|----------------|------|
| {{項目名}} | {{論理型}} | ENT-00X.属性名 / 派生 | {{ハイフン / 空白 / 0 等}} | {{意味・用途}} |

**出力フォーマット**: {{CSV / Excel / PDF のどの種別を出すか、使い分け条件}}

**ファイル名規則の方針**: {{可変部（対象年月・出力日時・利用者識別 等）と固定部の構成方針。最終命名規則は詳細設計}}

**文字コード・改行コード**: {{1 章の原則（例: UTF-8 BOM なし・LF）に沿うか、逸脱する場合の理由}}

**大量件数時の扱い**: {{同期ダウンロードの上限 / 超過時に非同期生成＋通知へ切替 / 分割ダウンロード 等の方針}}

**備考**: {{論理契約レベルの補足のみ。列幅・書式・ページ区切りの具体は詳細設計に回す}}

#### 3.1.3 通知仕様

##### UI-0XX {{通知名}}（通知）

**概要**: {{通知の目的・送出契機・関連 UC-NNN}}

**チャネル**: {{メール / SMS / プッシュ通知 / 社内チャット 等の種別。具体送信サービスはインフラ設計}}

**宛先の決まり方**: {{固定宛先 / ロール単位 / 利用者設定に従う / イベント主体に連動。宛先情報の源泉 ENT・属性}}

**送出契機**: {{ユーザ操作後 / スケジュール / イベント駆動 / 閾値超過。非同期境界をまたぐ場合の遅延許容}}

**本文の必須要素**:
- 差出人の方針
- 件名の方針
- 本文に必ず含める情報項目: {{対象期間 / 操作リンク / 相関 ID 等}}
- 最終文面は詳細設計で確定する。

**再送・重複抑止**: {{同一イベントの重複通知抑止方針、失敗時の再送条件・回数}}

**配信失敗時の扱い**: {{宛先不達・外部サービス障害時の記録・再送・運用者通知の方針}}

#### 3.1.4 CLI 仕様

##### UI-0XX {{CLI 名}}（CLI）

**概要**: {{CLI の目的・対象利用者（エンドユーザ / 運用者 / 開発者）・代表ユースケース・関連 UC-NNN}}

**呼出形式**: `command [subcommand] [options] [arguments]`（具体コマンド名は詳細設計で確定可。サブコマンド階層の意味はここで固める）

**サブコマンド一覧**:

| サブコマンド（論理） | 目的 | 起動 API | 関連 UC |
|------------------------|------|-----------|---------|
| {{sub1}} | {{目的}} | API-00X | UC-00X |

**オプション（フラグ）一覧**:

| 論理名 | 論理型 | 必須 | 既定値 | 値域・列挙 | 対応 ENT・属性 | バリデーション ID | 概要 |
|--------|--------|------|--------|-------------|------------------|---------------------|------|
| {{--name}} | {{論理型}} | ○ / △ / − | {{既定値}} | {{範囲・列挙値}} | ENT-00X.属性名 | VR-00X | {{意味}} |

**位置引数一覧**:

| 順序 | 論理名 | 論理型 | 必須 | 値域 | 対応 ENT・属性 | 概要 |
|------|--------|--------|------|------|------------------|------|
| 1 | {{name}} | {{論理型}} | ○ | {{範囲}} | ENT-00X.属性名 | {{意味}} |

**標準入力（stdin）**: {{利用する / しない。利用する場合の形式（行指向テキスト / JSON / CSV）と終端規約}}

**出力**:
- 標準出力（stdout）: {{人間可読 / 機械可読 / 両対応（例: `--json` で切替）。切替条件}}
- 標準エラー出力（stderr）: {{進捗・警告・エラーのどれを出すか}}
- ファイル出力: {{出力パスの決まり方（あれば）}}

**終了コード**:

| 終了コード | 意味 | 対応 ERR |
|-------------|------|-----------|
| 0 | 正常終了 | − |
| 1 | 入力不正 | ERR-001 |
| 2 | 認証失敗 | ERR-002 |
| 3 | 業務ルール違反 | ERR-006 |
| 4 | 外部依存失敗 | ERR-007 |
| 99 | 想定外の異常 | ERR-099 |

**対話挙動**: {{対話プロンプトの有無 / 非 TTY 時の挙動 / `--yes` 等の非対話強制フラグの方針}}

**環境変数・設定ファイル**: {{入力として解釈する環境変数の論理名と意味、設定ファイル読込経路の方針。機微情報を環境変数／ファイルで受けてよいか}}

**冪等性・副作用・dry-run**: {{破壊的操作の有無 / `--dry-run` 相当の可否 / 連続実行時の挙動}}

**認証・認可**: {{ログイン済みセッション参照 / トークンファイル / OS ユーザ連動 / 都度認証。権限判定の責務位置}}

**ログ・監査**: {{機微情報を stdout / stderr / ログに含めない原則と、監査要件（NFR-NNN）で取得すべき操作の対応}}

#### 3.1.5 音声インタフェース仕様（VUI / IVR）

##### UI-0XX {{音声 I/F 名}}（音声）

**概要**: {{目的・対象利用者・主チャネル（電話 / スマートスピーカー / 車載 等）・関連 UC-NNN}}

**起動ワード**: {{採用する / しない。採用時の固定方針（具体文字列は詳細設計）}}

**意図（intent）一覧**:

| 意図名 | 目的 | 典型発話パターンの種別 | スロット項目 | 起動 API | 関連 UC |
|---------|------|--------------------------|---------------|-----------|---------|
| {{intent_name}} | {{目的}} | {{自由発話 / 固定フレーズ / 選択肢回答}} | slot1, slot2 | API-00X | UC-00X |

**スロット（パラメータ）**:

| 意図 | スロット | 論理型 | 必須 | 値域 | 対応 ENT・属性 | バリデーション ID |
|------|----------|--------|------|------|------------------|---------------------|
| {{intent_name}} | {{slot_name}} | {{論理型}} | ○ / △ / − | {{範囲}} | ENT-00X.属性名 | VR-00X |

**応答方式**: {{音声合成（TTS）/ 事前収録 / 併用。切替条件}}

**応答文の必須要素**: {{確認復唱 / 確認番号 / 次操作案内 等。最終文言は詳細設計}}

**マルチモーダル出力**: {{画面併用時の画面側との役割分担（音声が主か補助か）}}

**対話制御**:
- 認識失敗・曖昧時: {{再確認 / 選択肢提示 / 有人オペレータへ切替。切替条件}}
- 発話待ちタイムアウト: {{方針（再案内 / 切断）}}

**認証**: {{声紋 / PIN / 外部認証（電話番号検証・SMS・アカウント紐付け）}}

**プライバシー・録音**:
- 録音の保持方針: {{保持する / しない / 条件付き}}
- 保持期間の方針
- 削除要求への対応方針
- 発話に機微情報が乗る前提のログ・マスキング方針

#### 3.1.6 チャット・メール等のテキストボット仕様

##### UI-0XX {{ボット名}}（チャット / メール）

**概要**: {{目的・利用チャネル（社内チャット / LINE / Slack / メール 等の種別）・関連 UC-NNN}}

**入力モデル**:
- 受信メッセージの形式: {{テキスト / 添付ファイル可否 / コマンド記法（`/command` 等）}}
- 意図抽出: {{固定コマンド解釈 / 自然文の意図分類}}
- 添付受信: {{受付ファイル種別・サイズ上限の方針}}

**意図（intent）一覧**:

| 意図名 | 目的 | 起動 API | 関連 UC |
|---------|------|-----------|---------|
| {{intent_name}} | {{目的}} | API-00X | UC-00X |

**出力モデル**:
- 返信メッセージの形式: {{プレーンテキスト / リッチコンポーネント（カード・ボタン・クイックリプライ）の方針}}
- プロアクティブ発信: {{ボット側から送る場合の送出契機}}

**会話状態**: {{ステートレス / ステートフル。ステートフルの場合の保持情報項目・有効期限}}

**エスカレーション**: {{自動応答から人間オペレータへの切替条件 / 引継ぎ時に渡すコンテキスト範囲}}

**認証・発信者同定**: {{チャネル側ユーザ ID を信用する範囲 / アカウント紐付け方法 / なりすまし対策方針}}

**スレッド／引用**: {{返信スレッドの保持方針 / 引用の有無 / メールの場合の Re: プレフィックス・Message-ID / In-Reply-To の扱い}}

### 3.2 API インタフェース仕様

#### API-001 {{名称}}

**概要**: {{何をする API か、1〜2 文。関連 UC・UI と LNK-NNN の紐付け}}

**エンドポイント**: `POST /resources`

**リクエスト**:

| 項目名（論理） | 論理型 | 必須 | 値域・列挙 | 対応 ENT・属性 | バリデーション ID | 概要 |
|-----------------|--------|------|-------------|------------------|---------------------|------|
| {{項目名}} | {{論理型}} | ○ / △ / − | {{範囲・列挙値}} | ENT-001.属性名 | VR-001 | {{意味}} |

**レスポンス**（成功時）:

| 項目名（論理） | 論理型 | 必須 | 源泉 | 概要 |
|-----------------|--------|------|------|------|
| {{項目名}} | {{論理型}} | ○ | ENT-001.属性名 / 派生 | {{意味}} |

リスト応答のページネーション方針: {{オフセット型 / カーソル型 / 全件返却（件数上限あり）。具体パラメータ名は詳細設計}}

**主要ステータス**:

| ステータス | 発生条件 | 対応 ERR |
|-------------|-----------|-----------|
| 201 | 正常登録 | − |
| 400 | 入力形式不正（VR 違反） | ERR-001 |
| 401 | 認証失敗 | ERR-002 |
| 403 | 認可エラー（権限不足） | ERR-003 |
| 409 | 一意性違反（同一キーの重複登録） | ERR-004 |
| 422 | 業務ルール違反（RULE-NNN 違反） | ERR-005 |
| 500 | システム内部エラー | ERR-099 |

**冪等性・同時実行**:
- 冪等性: {{冪等 / 非冪等。非冪等の場合の二重呼出抑止方針（クライアントリクエスト ID 受付 等）}}
- 同時実行: {{楽観ロック（更新トークン or バージョン属性）/ 悲観ロック / なし}}

**タイムアウト・リトライ方針**:
- タイムアウト区分: {{画面同期（短め）/ バッチ（長め）/ 非同期（指定なし）}}（具体秒数は詳細設計で確定）
- リトライ: {{不可 / サーバ側で自動再試行 / クライアント側で手動再試行可。再試行可の場合の条件と最大回数の方針}}

**認証・認可**:
- 認証: {{必須 / 不要 / 内部サービス間のみ}}
- 認可: {{テナント境界 / 組織境界 / ロール粒度 のどれで判定するか。特定ロール必須などの追加条件}}

**備考**: {{論理契約レベルの補足のみ。JSON スキーマ・フィールド命名規則は詳細設計に回す}}

### 3.3 システム間インタフェース仕様

#### SIF-001 {{連携名}}

**概要**: {{連携相手・目的・正本。要件側の SIF-NNN 記述との差分がないか確認}}

**連携方式の詳細**:
- 方式: {{ファイル / API / メッセージング}}
- ファイル連携の場合: アップロード場所の種別（画面アップロード / 共有フォルダ / S3 バケット種別）/ 想定エンコーディング（UTF-8 BOM なし 等）/ 改行コード / ファイル名規則の方針
- API 連携の場合: 相手側エンドポイント / 認証方式の種別（API キー / OAuth / 相互 TLS）
- メッセージング連携の場合: キュー or トピック / 順序保証の要否
- 具体の製品・プロトコル設定はインフラ設計側で確定する。

**レイアウト（受信／送信項目）**:

| 項目名（論理） | 論理型 | 必須 | 値域・列挙 | 対応 ENT・属性 | 備考 |
|-----------------|--------|------|-------------|------------------|------|
| {{項目名}} | {{論理型}} | ○ / △ / − | {{範囲・列挙値}} | ENT-009.属性名 | {{相手側固定仕様 / 自由設計 等}} |

**連携契機と頻度**: {{人手実行 / スケジュール（月次・日次）/ イベント駆動。頻度・件数の概算は要件側と整合}}

**失敗時の扱い**:
- 個別行エラー: {{当該行スキップ / 全件ロールバック / どの条件でどちらか}}
- ファイル全体エラー（形式不正等）: {{取り込み全体を中止 / 手動確認を促す}}
- 取り込み履歴（ENT-NNN）への記録: {{あり / なし。記録する項目}}
- リトライ可否: {{不可（手動で再実行）/ 自動リトライあり（条件・上限）}}

**権威と整合**:
- 正本: {{自システム / 外部システム / 双方で協調}}
- マスタの加筆・削除が双方で起きた場合の優先ルール: {{正本側の値で上書き / 相手側を参照のみ / 手動マージ 等}}

**備考**: {{運用上の注意（取り込み実行時間帯・相手システムのメンテナンス時間 等）を論理レベルで}}

### 3.4 ハードウェアインタフェース仕様（HIF-NNN）

要件側で `HIF-NNN` が「該当なし」と宣言されているプロジェクトでは本節も「該当なし」と明記して省略してよい。

#### HIF-001 {{機器名}}

**概要**: {{機器種別（スキャナ / プリンタ / IC カードリーダ / バーコードリーダ / カメラ / センサ / POS レジ 等）・目的・関連 UC-NNN}}

**接続方式の種別**: {{USB / Bluetooth / シリアル / ネットワーク越し / ドライバ経由 / OS 標準印刷スプール経由 等}}（具体製品・ドライバ・ポート番号はインフラ設計で確定）

**入出力データ形式**: {{機器から受け取る or 送るデータの論理形式。カード UID / バーコード文字列 / センサ値の単位・粒度 / 画像解像度方針 / 印刷ジョブ内容項目 / 制御コマンド種別}}

**トリガ・イベント**: {{機器操作がシステム側のどのイベント（API-NNN / 画面イベント）に対応するか}}

**機器未接続・異常時の扱い**:
- 画面・CLI 側の表示方針
- リトライ可否
- 手動復旧の導線（誰が何をするか）
- 対応 ERR-NNN

**校正・初期化**: {{操作主体（利用者 / 運用者）と頻度の方針}}

**機器固有の法規制**: {{規格適合・検定の有無。本ドキュメントで扱う範囲の宣言。詳細はインフラ設計・調達工程}}

### 3.5 通信インタフェース仕様（CIF-NNN）

`CIF-NNN` は通信経路（HTTPS / クラウド SDK 等）の粒度で要件定義書に記述済みのため、本ドキュメントでは新たな詳細を追加しない。2.4 節の紐付け表で、本ドキュメントの API・SIF・HIF との対応確認を行う。TLS バージョン・暗号スイート・証明書運用はインフラ設計で確定する。

---

## 4. エラー・例外方針

個別エラーは 3 章の各 I/F 仕様内で列挙する一方、横串のエラー分類・扱いは本章で決める。I/F ごとの挙動がばらつくとフロントエンド実装と利用者理解が破綻するため。

### 4.1 エラーカタログ

利用者向け挙動はチャネル別（画面 / 帳票 / 通知 / CLI / 音声 / チャット・メール / HIF）に方針を示す。全チャネル同じ挙動なら 1 行で十分、違う場合は該当チャネルを併記する。

| ERR-ID | 分類 | 代表的な原因 | 利用者向け挙動（チャネル別） | 再試行可否 | 監査・ログ方針 | 関連 API・UI・HIF |
|---------|------|----------------|--------------------------------|-------------|------------------|--------------------|
| ERR-001 | 入力不正 | 必須項目未入力、値域・形式違反 | 画面: フォーム再表示＋該当項目ハイライト / CLI: stderr にメッセージ＋終了コード 1 / 音声: 言い直し誘導 / ボット: 返信で項目指摘 | 修正後に可 | 通常ログ（監査不要） | API-001, UI-001 |
| ERR-002 | 認証 | セッション切れ、トークン不正 | 画面: ログイン画面へ遷移 / CLI: 終了コード 2＋再ログイン案内 / 音声: 本人確認やり直し / ボット: 再認証導線 | 再認証後に可 | 監査ログ | 全 API, UI-016 |
| ERR-003 | 認可 | 権限不足 | 画面: エラーメッセージ（遷移せず）/ CLI: 終了コード 2＋権限案内 / 他: 同等メッセージ | 権限付与まで不可 | 監査ログ | 全 API |
| ERR-004 | 競合 | 一意性違反、楽観ロックバージョン不一致 | 画面: 最新状態再取得のうえ再試行案内 / CLI: 差分提示＋終了コード 3 / 他: 同様 | 再取得後に可 | 監査ログ | 登録・更新 API |
| ERR-005 | 存在しない | 対象 ID が存在しない、削除済み | 画面: 404 または「対象が見つかりません」/ CLI: 終了コード 3＋メッセージ / 他: 同様 | 不可（URL / 引数見直し）| 通常ログ | 取得・更新・削除 API |
| ERR-006 | 業務ルール違反 | RULE-NNN 違反 | 画面: 違反内容を明示してフォーム再表示 / CLI: 終了コード 3＋違反 RULE 表示 / 音声: 理由を口頭通知 | 修正後に可 | 通常ログ | 該当 API |
| ERR-007 | 外部依存失敗 | SIF / CIF / HIF 先の障害・タイムアウト | 即時エラー / 再試行案内 / 縮退表示 / 機器切替（I/F ごとに方針） | I/F ごとに判定 | 監視アラート連携 | 該当 API・HIF |
| ERR-099 | システム内部 | 予期せぬ例外 | 画面: 汎用エラー画面＋相関 ID / CLI: 終了コード 99＋相関 ID / 音声: 短縮 ID を口頭＋SMS 送付 | 不可（問い合わせ） | 監視アラート連携 | 全 I/F |

### 4.2 例外フローとユースケースの整合
- 各 `usecase.md` の例外フローに登場する分岐条件をすべて `ERR-NNN` と対応付ける。分類外のものがあれば、分類を追加するかユースケース側の扱いを見直す。
- 画面表示方針（モーダル / トースト / ページ遷移 / フォーム再表示）は `ERR-NNN` の分類単位で決め、個別 I/F で勝手に別方針を採らない。最終文面は詳細設計で確定する。

### 4.3 外部依存失敗の方針（ERR-007 詳細）
- 同期呼出中に相手側が遅延・失敗した場合: {{即時エラー / 所定時間まで待機して失敗 / 縮退（古いデータで応答）}}
- 非同期経路で失敗した場合: {{DLQ 行き / 管理者通知 / 手動再実行待ち}}
- 画面側の見せ方: {{ユーザに再試行を促す / 管理者問い合わせを促す / 機能単位の縮退表示}}

### 4.4 相関 ID と追跡性
- すべてのエラー応答に相関 ID（trace ID 相当）を含める。値の採番方法・伝搬経路の詳細はインフラ設計で確定する。
- 利用者画面に相関 ID を表示する条件（ERR-099 のみ / 全エラーに短縮 ID を表示 等）を定める。
- 監査要件（NFR-NNN 責任追跡性）との紐付け: {{該当 NFR-ID と結びつける}}

---

## 5. セキュリティ考慮

本章はセキュリティの論理契約レベルの決定に閉じる。具体の製品選定・設定値（暗号スイート・TLS バージョン・WAF ルール）はインフラ設計で決める。

- **認証の責務位置（再掲）**: {{アーキ側の決定を再掲。IdP 委譲 / ゲートウェイ集約 / 自前}}
- **認可判定の粒度**: {{テナント境界 / 組織境界 / ロール粒度 のどこで判定するか。個別 API への適用方針は 3 章に記載}}
- **機微情報の扱い**:
  - 機微扱い項目: {{個人情報・認証情報・金額 等}}
  - リクエストでの扱い: {{必要時のみ送信 / 平文可否 / マスキングの有無}}
  - レスポンスでの扱い: {{全部返す / マスキング / 返さない}}
  - ログでの扱い: {{除外 / マスキング / 通常ログ可}}
- **入力の正規化・サニタイズ方針**:
  - 画面入力: {{文字コード・ユニコード正規化・前後空白・全半角統一の有無}}
  - API 側: {{画面通過後も再検証する前提。パラメタライズドクエリ使用原則。出力エスケープのコンテキスト別原則}}
- **認証情報と秘密情報の授受**:
  - トークン・API キー・署名鍵・パスワードがリクエスト／レスポンス／ログに現れてよい場所
  - ログに出さない項目の具体列挙
- **監査ログの取得点**: {{「誰が／いつ／どの API を／どの対象に／どの結果で」実行したかを記録する I/F の列挙。NFR（責任追跡性）との紐付け}}
- **同時実行・なりすまし防止**:
  - 楽観ロック: {{更新トークン or バージョン属性 のどれを使うか方針}}
  - CSRF: {{対策方針（トークン方式 / SameSite Cookie 等の種別）}}
  - レート制限: {{IP 単位 / トークン単位 / 機能単位 のどれで掛けるかの原則。具体値はインフラ設計}}

---

## 6. 注意事項

注意事項は単一の表ではなく、以下 3 区分で整理する。混在させると読み手の対応先が曖昧になる。

### 6.1 リスク・懸念点

| RISK-ID | 内容 | 影響範囲（UI / API / SIF / HIF / CIF / ERR / 業務） | 顕在化条件 | 暫定対応 | 詳細設計・インフラ設計への申し送り |
|---------|------|------------------------------------------------------|-------------|-----------|--------------------------------------|
| RISK-001 | クライアント検証（VR）とサーバ側再検証のズレ | UI-NNN / API-NNN 横断 | 画面・CLI・ボットの片側改修が先行した場合 | VR-NNN をデータ設計 RULE-NNN に紐付けて両者で共有 | クライアント／サーバのバリデーション自動生成の検討（詳細設計） |
| RISK-002 | 大量件数検索の性能と UX のトレードオフ（ページネーション方式未確定） | API-00X 一覧系 | 想定件数を超える結果が発生 | カーソル型を基本方針とし、オフセット型は小規模一覧のみ | カーソル構造の具体仕様は詳細設計 |
| RISK-003 | 外部システム SIF-NNN のレイアウト変更への追随 | SIF-NNN | 相手側のシステム改修時 | 受信時にレイアウトバージョンを記録し差分検知 | 自動検知の実装方法は詳細設計 |
| RISK-004 | 機微情報のログ混入（デバッグログ・CLI の stdout / 音声録音 への混入） | 全 API / 人間向け I/F | 開発・運用者が誤って平文出力 | ログ出力ユーティリティでマスキングをハードコード | フレームワーク側での一括マスキング機構（詳細設計） |
| RISK-005 | 人間向け I/F 横断での用語・挙動不一致 | UI-NNN 横断 | 複数種別（画面 / CLI / 音声 / ボット）から同じ業務動作を提供する場合 | 用語辞書と API 共通化で揃える | 種別別の UI 担当者間の定例レビュー（詳細設計） |
| RISK-006 | HIF の機器未接続・校正ズレ | HIF-NNN / 業務 | 機器入替え後・校正期限切れ | 画面・CLI に機器状態の表示と校正期限の警告を出す | 機器監視の自動化（インフラ設計） |
| RISK-007 | 要件未確定のまま進めた I/F 前提 | 該当 I/F | 想定と異なる要求が出た場合 | 仮置きの方針で進行。確認結果により再調整 | 確認先: {{ステークホルダー名}} |

リスクの典型カテゴリ:
- クライアント側とサーバ側のバリデーション齟齬
- CLI のシェル展開・引用符の扱い
- 音声認識の誤認識・方言・雑音環境下での信頼度低下
- チャット・メールボットでのなりすまし・スレッド混線
- ページネーションの総件数・性能トレードオフ
- 冪等性の担保方針の未確定
- 外部システムのレイアウト変更
- ハードウェアの未接続・異常・ドライバ差異
- 非同期境界をまたぐ完了通知の遅延
- 機微情報のログ混入（stdout・録音を含む）
- 大量ファイル取り込み時のタイムアウト・部分失敗
- エラー分類の粒度と要件例外フローの噛み合わせ
- 人間向け I/F 種別間の用語・挙動不一致

### 6.2 代替案と採らない理由

| ALT-ID | 対象判断 | 代替案概要 | 採らない理由 | 再検討条件 |
|--------|----------|------------|---------------|-------------|
| ALT-001 | API 呼出様式 | GraphQL を採用 | 現要件では取得パターンの柔軟性より、API の責務の明確さと監査性が優先される。REST のリソース指向で十分 | 画面側で大量の取得パターンが発生し、アンダー／オーバーフェッチが顕在化した場合 |
| ALT-002 | ページネーション方式 | 全件返却（件数上限なし） | 大量データ時の性能と通信量が NFR-00X を侵食する | 対象データの件数上限が業務ルール上確定した場合 |
| ALT-003 | エラー応答構造 | HTTP ステータスのみ | 利用者にエラー理由を返せないとフロントエンド実装が破綻する。機械可読コードは必須 | 対象クライアントが限定され、フロントエンドで個別処理する場合 |
| ALT-004 | 冪等性の担保位置 | クライアント側のリトライ抑止のみに委ねる | ネットワーク起因の二重送信を防げず、重複登録のリスクが残る | 業務上、重複登録の影響が無視できるほど軽微な場合 |
| ALT-005 | SIF 取り込み失敗時 | 常に全件ロールバック | 一部行のみエラーで全体失敗は業務停止につながる | エラー行があった場合の全体影響が法令・契約上許容できない場合 |
| ALT-006 | CLI の出力形式 | 人間可読のみ | 監視・自動化から扱えず、運用側の利用に支障 | 将来的な運用自動化が不要と確定した場合 |
| ALT-007 | 音声認識失敗時の対話戦略 | 即有人エスカレーション | オペレータ負荷が高く、軽度の誤認識でも切替が発生 | 自動対話で解決できる範囲を絞る方針に合意できた場合 |
| ALT-008 | ボットの会話状態 | 常にステートフル | セッション保持のコスト・整合が重く、単発問い合わせには過剰 | 複数ターン対話が主流ユースケースになった場合 |
| ALT-009 | HIF 機器異常時 | 常に処理中断 | 一時的な機器異常で業務全停止になり、受注受付などを阻害する | 機器が業務クリティカルで縮退運転を許容できない場合 |

メモ:
- 最低でも上記のうち本プロジェクトで該当する判断は検討の痕跡を残す。該当しないものは「検討不要と判断した理由」を 1 行で記す。
- 採用しない I/F 種別（例: 音声 / HIF を採用しないプロジェクト）に関わる行は表から削ってよい。
- インフラ寄りの判断（TLS バージョン・WAF 製品・LB 振分けアルゴリズム）は本表に含めず、インフラ設計の代替案検討で扱う。

### 6.3 後続工程への申し送り事項

#### 詳細設計（機能・画面 / CLI / 音声 / ボット単位 / API 精密仕様）への申し送り

| HO-D-ID | 内容 | 想定選択肢 | 決定責任者 / 相談先 |
|---------|------|------------|----------------------|
| HO-D-001 | 画面 UI レイアウト・コンポーネント分割・配色 | {{候補}} | {{担当}} |
| HO-D-002 | CLI の具体コマンド名・サブコマンド階層・フラグ命名規則・ヘルプ文言 | {{候補}} | {{担当}} |
| HO-D-003 | 音声 I/F の応答文テンプレート・認識モデルのチューニング・声質 | {{候補}} | {{担当}} |
| HO-D-004 | チャット／メールボットのメッセージ文面・リッチコンポーネントレイアウト | {{候補}} | {{担当}} |
| HO-D-005 | 帳票の列幅・書式・ページ区切り | {{候補}} | {{担当}} |
| HO-D-006 | 通知本文の最終文面・多言語対応 | {{候補}} | {{担当}} |
| HO-D-007 | API の具体 JSON スキーマ・フィールド命名規則・バージョニング記法 | snake_case / camelCase / `/v1/…` 等 | {{担当}} |
| HO-D-008 | ページネーションの具体パラメータ名・カーソルトークン構造 | {{候補}} | {{担当}} |
| HO-D-009 | バリデーションメッセージの最終文言（多言語含む） | {{候補}} | {{担当}} |
| HO-D-010 | タイムアウト具体秒数（画面同期 / バッチ / 外部連携） | {{候補}} | {{担当}} |
| HO-D-011 | 個別 API のキャッシュ戦略 | {{候補}} | {{担当}} |

#### インフラ設計への申し送り

| HO-I-ID | 内容 | 想定選択肢 | 決定責任者 / 相談先 |
|---------|------|------------|----------------------|
| HO-I-001 | API ゲートウェイ製品・WAF 製品・LB 製品の選定 | {{候補}} | {{担当}} |
| HO-I-002 | TLS 設定値・暗号スイート・証明書運用 | {{候補}} | {{担当}} |
| HO-I-003 | レート制限の具体値（QPS / バースト許容） | {{候補}} | {{担当}} |
| HO-I-004 | ネットワークタイムアウト値とヘルスチェック条件 | {{候補}} | {{担当}} |
| HO-I-005 | CDN 配信ルール・キャッシュ階層の物理配置 | {{候補}} | {{担当}} |
| HO-I-006 | 音声・通知・メール送信サービス製品選定（TTS / SMS / メール配信基盤） | {{候補}} | {{担当}} |
| HO-I-007 | HIF 関連の機器ドライバ・ポート設定・機器監視 | {{候補}} | {{担当}} |

---

## 7. 参照ドキュメント

- 要件定義書（機能要件）: [`docs/requirements/functional.md`](../../requirements/functional.md)
- 要件定義書（非機能要件）: [`docs/requirements/non-functional.md`](../../requirements/non-functional.md)
- ユースケース詳細: `docs/requirements/usecase/UC-NNN/usecase.md`
- アーキテクチャ設計書: [`docs/design/architecture/architecture.md`](../architecture/architecture.md)
- データ設計書: [`docs/design/data/data-design.md`](../data/data-design.md)
- {{その他参照資料（社内標準・API 設計ガイドライン・既存 I/F 仕様書 等）}}
